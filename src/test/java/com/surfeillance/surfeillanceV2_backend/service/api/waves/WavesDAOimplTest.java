package com.surfeillance.surfeillanceV2_backend.service.api.waves;

/*
import com.surfeillance.surfeillanceV2_backend.repository.ForecastRepository;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.context.annotation.Import;
import org.springframework.web.client.RestTemplate;


@DataJpaTest
@Import(WavesDAOimpl.class)
class WavesDAOimplTest {

    @Autowired
    private WavesDAOimpl wavesDAO;

    @Mock
    private RestTemplate restTemplate;

    @BeforeEach
    void setUp(){
        MockitoAnnotations.openMocks(this);

    }

    @Test
    void getWaveData() {
    }
}*/
